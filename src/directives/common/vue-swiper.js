import Vue from 'vue';import '../../libs/js/plugin/swiper.min';Vue.directive('swiper', {	bind: function (el, binding) {		binding.handler = function () {			let $this = $(el);			let swiperBanner;			let pagination = $this.data('pagination');			let autoplay = $this.data('autoplay');			let autopalytime = $this.data('autoplaytime');			let paginationClickable = $this.data('paginationclickable');			let spaceBetween = $this.data('margin');			if (pagination && autoplay) {				swiperBanner = new Swiper($this, {					slidesPerView: 'auto',					lazyLoading: true,					loop: true,					pagination: '.swiper-pagination',					autoplay: autopalytime				});			} else {				if (pagination) {					swiperBanner = new Swiper($this, {						slidesPerView: 'auto',						lazyLoading: true,						loop: true,						pagination: '.swiper-pagination'					});				} else if (autoplay) {					swiperBanner = new Swiper($this, {						slidesPerView: 'auto',						lazyLoading: true,						loop: true,						autoplay: autopalytime					});				} else if (paginationClickable) {					swiperBanner = new Swiper($this, {						slidesPerView: 'auto',						lazyLoading: true,						paginationClickable: paginationClickable,						spaceBetween: spaceBetween || 0					});				} else if (spaceBetween) {					swiperBanner = new Swiper($this, {						slidesPerView: 'auto',						lazyLoading: true,						spaceBetween: spaceBetween					});				} else {					swiperBanner = new Swiper($this, {						slidesPerView: 'auto',						lazyLoading: true,						loop: true					});				}			}		};		setTimeout(function () {			binding.handler();		}, 60);	}});
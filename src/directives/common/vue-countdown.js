import Vue from 'vue';Vue.directive('timeout', {	bind: function (el,binding) {        let $this = $(el),            $timeout = $this.find('.J-timeout'),            value = binding.value,            SysSecond = value.time;        if(SysSecond > 0){            const InterValObj = window.setInterval(function(){                if (SysSecond > 0) {                    SysSecond--;                    let timeHtml,                        second = Math.floor(SysSecond % 60),                        minute = Math.floor((SysSecond / 60) % 60),                        hour = Math.floor((SysSecond / 3600 % 24)),                        day = Math.floor((SysSecond / 3600 / 24));                    day = day > 9 ? day : '00';                    hour = hour > 9 ? hour : '0' + hour;                    minute = minute > 9 ? minute : '0' + minute;                    second = second > 9 ? second : '0' + second;                    if (day && day !== '00') {                        timeHtml = '<span>'+ day +'</span>:' + '<span>'+ hour +'</span>:' + '<span>'+ minute +'</span>:' + '<span>'+ second +'</span>'                    } else {                        timeHtml = '<span>'+ hour +'</span>:' + '<span>'+ minute +'</span>:' + '<span>'+ second +'</span>'                    }                    $timeout.html(timeHtml);                } else {                    window.clearInterval(InterValObj);                    window.location.reload();                }            },1000);        }	}});